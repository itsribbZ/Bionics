# Bionics UE5 Modules — Domain-specific expert systems
#
# Each module makes Bionics an expert in a specific UE5 editor domain:
#   animgraph/ — AnimGraph editor (nodes, pins, state machines, compilation)
#
# Modules are auto-loaded by templates.py on import to register
# their ActionTemplate subclasses in the global template registry.
