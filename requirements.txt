anthropic>=0.50.0,<2.0.0
PyQt6>=6.7.0,<7.0.0
mss>=9.0.0,<10.0.0
pyautogui>=0.9.54,<1.0.0
pynput>=1.7.7,<2.0.0
keyboard>=0.13.5,<1.0.0
PyMuPDF>=1.24.0,<2.0.0
Pillow>=10.4.0,<12.0.0
pyyaml>=6.0.2,<7.0.0
opencv-python>=4.10.0,<5.0.0
numpy>=1.26.0,<3.0.0
requests>=2.32.0,<3.0.0
pywinauto>=0.6.8,<1.0.0 ; sys_platform == 'win32'

# Bionics v0.5.1 — MCP server + CLI integration
fastmcp>=2.11.0,<4.0.0   # MCP server (Claude Code/Cursor/Windsurf)
pydantic>=2.9.0,<3.0.0   # Tool schemas + validation

# Optional — tools degrade gracefully if missing
pyperclip>=1.9.0,<2.0.0  # clipboard_get / clipboard_set tools
psutil>=6.0.0,<8.0.0     # list_processes tool (matches pyproject.toml)
# pytesseract>=0.3.13    # ocr_screen tool (requires Tesseract binary installed)
